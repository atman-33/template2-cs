<UserControl
    x:Class="Template2.WPF.Views.Sample011View"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:bindings="clr-namespace:Template2.WPF.ExtendedBindings"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:prism="http://prismlibrary.com/"
    prism:ViewModelLocator.AutoWireViewModel="True">

    <Grid Margin="{StaticResource gridMargin}">

        <DockPanel>

            <Label Content="ドラッグ＆ドロップで並び替え可能です。" DockPanel.Dock="Top" />

            <!--  DataGridをドラッグ＆ドロップした際のポップアップ表示  -->
            <Popup
                x:Name="DraggingPopup"
                AllowsTransparency="True"
                IsHitTestVisible="False"
                Placement="RelativePoint"
                PlacementTarget="{Binding ElementName=WorkerGroupMstEntitiesName}">
                <!--  Popup construction Use properties of DraggedObject inside for Binding  -->
                <TextBlock Text="{Binding Path=WorkerGroupName}" />
            </Popup>

            <materialDesign:Card
                Grid.Row="0"
                Grid.Column="0"
                Margin="5"
                HorizontalAlignment="Left">

                <DataGrid
                    x:Name="WorkerGroupMstEntitiesName"
                    Margin="5"
                    bindings:ReorderableDataGridRowBehavior.Enabled="True"
                    bindings:ReorderableDataGridRowBehavior.PopupControl="{Binding ElementName=DraggingPopup}"
                    AllowDrop="True"
                    Cursor="Hand"
                    ItemsSource="{Binding WorkerGroupMstEntities}"
                    SelectionMode="Single">

                    <DataGrid.Columns>
                        <DataGridTextColumn
                            Binding="{Binding WorkerGroupCode}"
                            Header="作業者グループコード"
                            IsReadOnly="True" />
                        <DataGridTextColumn
                            MinWidth="400"
                            Binding="{Binding WorkerGroupName}"
                            Header="作業者グループ名称"
                            IsReadOnly="True" />
                    </DataGrid.Columns>

                </DataGrid>
            </materialDesign:Card>
        </DockPanel>

    </Grid>
</UserControl>
