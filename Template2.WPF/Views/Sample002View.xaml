<UserControl x:Class="Template2.WPF.Views.Sample002View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid Margin="{StaticResource gridMargin}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0" Margin="5" HorizontalAlignment="Left">
            <Button
                  Style="{StaticResource MaterialDesignRaisedLightButton}"
                  Width="100"
                  Content="テーブル変換"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.Column="0" Margin="5">
            <!-- マトリックステーブル -->
            <DataGrid Width="400"
                      Height="600"
                      ItemsSource="{Binding XXX}">
            </DataGrid>
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.Column="1" Margin="5">
            <!-- ストレートテーブル -->
            <DataGrid Width="400"
                      Height="600">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="担当者" Binding="{Binding XXX}"/>
                    <DataGridTextColumn Header="曜日番号" Binding="{Binding XXX}"/>
                    <DataGridTextColumn Header="勤務時間" Binding="{Binding XXX}"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>

    </Grid>
</UserControl>
